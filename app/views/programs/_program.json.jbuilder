json.extract! program, :id, :name, :image, :lecturers, :description, :level, :prerequisite, :literature, :syllabus, :hours, :skills, :workload, :faq, :capacity, :studyboard, :faculty, :created_at, :updated_at
json.url program_url(program, format: :json)
